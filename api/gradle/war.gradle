apply plugin: "war"

bootWar {
    mainClass = "es.nunsys.responsive.ResponsiveApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
